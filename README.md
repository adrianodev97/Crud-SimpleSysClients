# Sistema Clientes - CRUD

Sistema de gestão de clientes.

## Rodando o projeto localmente

- Executar o comando `npm i` para instalar as dependências.

- Executar o comando `npm run start` para executar o servidor.
